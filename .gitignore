# Go build output
/bin/
*.test
*.out
dist/

# Build artifacts (CLIs in build/, Lambda zips in terraform/build/)
/build/
/terraform/build/
cmd/notifier/bootstrap
cmd/notifier/*.zip
terraform/*.zip
terraform/bootstrap

# Detector report artifacts
report.json
report.md

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!terraform/example.tfvars

# Nested sub-repos (their own git repos — never tracked here)
/dummy-infra/
/slacked/

# Editor/OS
.DS_Store
